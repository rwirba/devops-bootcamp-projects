apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nexus-data-pvc
  namespace: ezlearn-cicd-ns
spec:
  accessModes: [ "ReadWriteOnce" ]
  storageClassName: manual
  resources:
    requests: { storage: 20Gi }
  selector:
    matchLabels: { pv: nexus-data }