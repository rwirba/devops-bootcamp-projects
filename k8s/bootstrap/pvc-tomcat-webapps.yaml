apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tomcat-webapps-pvc
  namespace: ezlearn-dev-ns
spec:
  accessModes: [ "ReadWriteOnce" ]
  storageClassName: manual
  resources:
    requests: { storage: 5Gi }
  selector:
    matchLabels: { pv: tomcat-webapps }