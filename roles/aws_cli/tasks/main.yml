---
- name: Update apt package index
  apt:
    update_cache: yes

- name: Install base packages
  apt:
    name:
      - python3-pip
      - jq
      - unzip
    state: present

- name: Install AWS CLI v2 (official method)
  block:
    - name: Download AWS CLI
      get_url:
        url: https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip
        dest: /tmp/awscliv2.zip
      
    - name: Unzip AWS CLI
      unarchive:
        src: /tmp/awscliv2.zip
        dest: /tmp
        remote_src: yes

    - name: Install AWS CLI
      command: /tmp/aws/install
      args:
        creates: /usr/local/bin/aws

    - name: Clean up installation files
      file:
        path: /tmp/awscliv2.zip
        state: absent

- name: Upgrade Python AWS packages
  pip:
    name:
      - boto3
      - botocore
    state: latest
    executable: pip3

- name: Install Amazon AWS Ansible collection
  command: ansible-galaxy collection install amazon.aws --force
  args:
    creates: "{{ ansible_collections_path }}/amazon/aws"

- name: Verify installations
  block:
    - name: Check AWS CLI version
      command: aws --version
      register: aws_version
      changed_when: false

    - name: Check Python packages
      command: pip3 show boto3 botocore
      register: pip_packages
      changed_when: false

    - name: Check Ansible collection
      command: ansible-galaxy collection list amazon.aws
      register: ansible_collection
      changed_when: false

    - name: Display versions
      debug:
        msg: |
          AWS CLI: {{ aws_version.stdout }}
          Python Packages:
          {{ pip_packages.stdout }}
          Ansible Collection:
          {{ ansible_collection.stdout }}